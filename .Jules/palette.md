# Palette UX Journal

## 2026-07-28 - Enhancing User Safety with Accessible Confirmations
**Learning:** Replaces raw standard `window.confirm` with custom, keyboard-accessible transitions and styled alerts to improve system feel and screen-reader compatibility.
**Action:** Replace `window.confirm` with a fully-styled, keyboard-accessible `ConfirmDialog` in pages with destructive actions.
