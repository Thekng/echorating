## 2026-09-23 - Search Input & Clear Button Accessibility
**Learning:** Icon-only clear buttons in search bars require explicit `aria-label="Clear search"` and `type="button"` attributes to ensure screen readers announce their function and prevent default form submit behavior.
**Action:** Always inspect search components for missing accessible labels on inputs and clear action triggers.
